let firstName = "code";
let middleName = "with";
let lastName = "Ashith";

const fullName = firstName + " " + middleName + " " + lastName;

console.log(fullName);

// using string literal
const fullNameUsingLiteral = `${firstName} ${middleName} ${lastName}`;
console.log(fullNameUsingLiteral);


